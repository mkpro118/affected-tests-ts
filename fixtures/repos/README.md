# Git Fixture Repositories

Future integration tests will place self-contained Git repositories here to
exercise merge-base ranges, rename detection, deletions, ignored files, and
real command output.
