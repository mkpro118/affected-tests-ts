# Affected Tests Fixtures

This tree holds deterministic test assets for the affected-tests-ts CLI.

`repos/` is reserved for real Git fixture repositories used by binary-level
integration tests. `vfs/` is reserved for static layouts loaded by unit tests
through virtual filesystem adapters.
